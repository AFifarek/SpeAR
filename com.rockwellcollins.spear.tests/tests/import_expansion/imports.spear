Specification simple

Imports:
	import "import_definitions2.spear"
	import "import_definitions.spear"

Constants:
	c2 : real = 5.2

Inputs:
	a : typeA
	b : real

Outputs:
	z : real

State:
	q : real

Macros:
	m1 : real = c1 + c2

Assumptions:
	a0 = a > 0.0
	a1 = b <= 0.0

DerivedRequirements:
	qr0 = q == a + b
	zr0 = z == a + b + c1 + m1 
	
Requirements:
	p0 = z - q > 0.0