Specification compute_time

Inputs:
	l : int
	m : int
	r : int

Outputs:
	time : int

Assumptions:
	al = 0 <= l and l <= 9
	am = 0 <= m and m <= 9
	ar = 0 <= r and r <= 9

//this section is mandatory
DerivedRequirements:
	dr0 = time == (l * 60) + (m * 10) + r

//this section is mandatory
Requirements:
	p0 = 0 <= time and time <= 639

